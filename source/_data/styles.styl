/* ================================================================
   Wisteria Tech Docs — Docs-style Brand (Tailwind / Vercel inspired)
   NexT Pisces theme override
   ================================================================ */

// ----------------------------------------------------------
// Colors — calm docs palette with light/dark variants
// ----------------------------------------------------------
:root {
  --wt-primary: #2563eb;
  --wt-primary-hover: #1d4ed8;
  --wt-primary-soft: #dbeafe;
  --wt-primary-light: #eff6ff;
  --wt-primary-shadow: rgba(37, 99, 235, 0.25);
  --wt-text: #111827;
  --wt-body-text: #334155;
  --wt-muted: #64748b;
  --wt-menu-text: #475569;
  --wt-border: #e2e8f0;
  --wt-bg: #ffffff;
  --wt-page-bg: #f8fafc;
  --wt-code-bg: #eff6ff;
  --wt-code-text: #1e40af;
  --wt-code-block-bg: #0f172a;
  --wt-code-caption: #64748b;
  --wt-table-alt-bg: #f8fafc;
  --wt-scrollbar: #cbd5e1;
  --wt-scrollbar-hover: #94a3b8;
  --wt-brand-bg: #ffffff;
  --wt-brand-border: #e2e8f0;
  --wt-brand-primary: #2563eb;
  --wt-brand-primary-hover: #1d4ed8;
  --wt-brand-muted: #64748b;
  --wt-brand-shadow: rgba(37, 99, 235, 0.25);
  --theme-color: var(--wt-primary);
  --brand-color: var(--wt-primary);
  --brand-hover-color: var(--wt-primary-hover);
}

@media (prefers-color-scheme: dark) {
  :root {
    --wt-primary: #60a5fa;
    --wt-primary-hover: #93c5fd;
    --wt-primary-soft: rgba(96, 165, 250, 0.22);
    --wt-primary-light: rgba(96, 165, 250, 0.12);
    --wt-primary-shadow: rgba(96, 165, 250, 0.24);
    --wt-text: #e5e7eb;
    --wt-body-text: #cbd5e1;
    --wt-muted: #94a3b8;
    --wt-menu-text: #cbd5e1;
    --wt-border: #263244;
    --wt-bg: #111827;
    --wt-page-bg: #0b1120;
    --wt-code-bg: rgba(96, 165, 250, 0.16);
    --wt-code-text: #bfdbfe;
    --wt-code-block-bg: #020617;
    --wt-code-caption: #94a3b8;
    --wt-table-alt-bg: rgba(148, 163, 184, 0.08);
    --wt-scrollbar: #334155;
    --wt-scrollbar-hover: #475569;
    --wt-brand-bg: #ffffff;
    --wt-brand-border: #e2e8f0;
    --wt-brand-primary: #2563eb;
    --wt-brand-primary-hover: #1d4ed8;
    --wt-brand-muted: #64748b;
    --wt-brand-shadow: rgba(37, 99, 235, 0.25);
    color-scheme: dark;
  }
}

$brand-primary        = var(--wt-primary)
$brand-primary-hover  = var(--wt-primary-hover)
$brand-primary-soft   = var(--wt-primary-soft)
$brand-primary-light  = var(--wt-primary-light)
$brand-primary-shadow = var(--wt-primary-shadow)
$brand-text           = var(--wt-text)
$brand-body-text      = var(--wt-body-text)
$brand-muted          = var(--wt-muted)
$brand-menu-text      = var(--wt-menu-text)
$brand-border         = var(--wt-border)
$brand-bg             = var(--wt-bg)
$brand-page-bg        = var(--wt-page-bg)
$brand-code-bg        = var(--wt-code-bg)
$brand-code-text      = var(--wt-code-text)
$brand-code-block-bg  = var(--wt-code-block-bg)
$brand-code-caption   = var(--wt-code-caption)
$brand-table-alt-bg   = var(--wt-table-alt-bg)
$brand-scrollbar      = var(--wt-scrollbar)
$brand-scrollbar-hover = var(--wt-scrollbar-hover)
$brand-logo-bg        = var(--wt-brand-bg)
$brand-logo-border    = var(--wt-brand-border)
$brand-logo-primary   = var(--wt-brand-primary)
$brand-logo-primary-hover = var(--wt-brand-primary-hover)
$brand-logo-muted     = var(--wt-brand-muted)
$brand-logo-shadow    = var(--wt-brand-shadow)
$brand-radius         = 6px
$brand-radius-sm      = 4px

// ----------------------------------------------------------
// 1. Global
// ----------------------------------------------------------
body {
  background: $brand-page-bg;
  color: $brand-text;
  font-family: -apple-system, BlinkMacSystemFont, "Inter", "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

// ----------------------------------------------------------
// 2. Site branding — white card with blue brand mark
// ----------------------------------------------------------
.site-brand-container,
.column .site-brand-container {
  background: $brand-logo-bg !important;
  border-bottom: 1px solid $brand-logo-border;
  padding: 18px 18px 16px;
}

.site-meta {
  padding: 0;
}

.brand {
  align-items: center;
  color: $brand-logo-primary !important;
  display: inline-flex;
  gap: 10px;
  line-height: 1.2;
}

.brand:hover {
  color: $brand-logo-primary-hover !important;
}

.brand .logo-line {
  display: none;
}

.brand::before {
  align-items: center;
  background: $brand-logo-primary;
  border-radius: 9px;
  box-shadow: 0 8px 18px $brand-logo-shadow;
  color: #ffffff;
  content: "W";
  display: inline-flex;
  flex: 0 0 34px;
  font-size: 0.98rem;
  font-weight: 800;
  height: 34px;
  justify-content: center;
  letter-spacing: 0;
  width: 34px;
}

.site-title {
  color: $brand-logo-primary;
  font-weight: 700;
  font-size: 1rem;
  letter-spacing: 0;
  line-height: 1.25;
}

.brand .site-title,
.brand:hover .site-title {
  color: $brand-logo-primary;
}

.site-subtitle {
  color: $brand-logo-muted;
  margin: 8px auto 0;
  max-width: 190px;
  font-size: 0.72rem;
  font-weight: 400;
  line-height: 1.5;
}

// ----------------------------------------------------------
// 3. Menu — clean nav links, dark bg
// ----------------------------------------------------------
.site-nav {
  border-top: 1px solid $brand-border;
  padding-top: 12px;
  margin-top: 10px;
}

.site-nav .menu-item {
  margin: 1px 0;
}

.site-nav .menu-item a {
  color: $brand-menu-text;
  font-weight: 500;
  font-size: 0.875rem;
  padding: 6px 12px;
  border-radius: $brand-radius-sm;
  transition: all 150ms ease;
  display: block;
}

.site-nav .menu-item a:hover {
  color: $brand-primary;
  background: $brand-primary-light;
}

.site-nav .menu-item-active a {
  color: $brand-primary;
  background: $brand-primary-light;
  border-left: none;
  font-weight: 600;
}

.menu-item .badge {
  background: $brand-primary-soft;
  color: $brand-primary;
  font-weight: 500;
}

// ----------------------------------------------------------
// 4. Sidebar — flat, clean, no shadows
// ----------------------------------------------------------
.sidebar-inner {
  background: $brand-bg;
  border-radius: 0;
  box-shadow: none;
  border: none;
  border-right: 1px solid $brand-border;
  padding: 0;
  overflow: visible;
}

.sidebar {
  background: transparent;
}

// ----------------------------------------------------------
// 5. Sidebar panels — docs-nav style
// ----------------------------------------------------------
.sidebar-panel {
  padding: 0 20px 20px 20px;
}

.links-of-blogroll {
  margin-top: 20px;
  padding-top: 16px;
  border-top: 1px solid $brand-border;
}

.links-of-blogroll-title {
  font-size: 0.7rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: $brand-muted;
  margin: 0 0 8px 0;
  padding: 0;
  text-align: left;
}

.links-of-blogroll-list {
  list-style: none;
  margin: 0;
  padding: 0;
}

.links-of-blogroll-item {
  margin: 0;
  padding: 0;
}

.links-of-blogroll-item a {
  display: block;
  color: $brand-menu-text;
  font-size: 0.825rem;
  padding: 4px 8px;
  border-radius: $brand-radius-sm;
  transition: all 120ms ease;
  line-height: 1.4;
  white-space: normal;
  overflow: visible;
  border-left: 2px solid transparent;
  text-align: left;
}

.links-of-blogroll-item a:hover {
  color: $brand-primary;
  background: $brand-primary-light;
  border-left-color: $brand-primary;
}

// Sidebar state (posts/categories/tags count)
.site-state {
  padding: 4px 0;
}

.site-state-item {
  padding: 0 8px;
}

.site-state-item a {
  color: $brand-menu-text;
  font-size: 0.8rem;
  border-bottom: none;
}

.site-state-item a:hover {
  color: $brand-primary;
}

.site-state-item-count {
  font-weight: 700;
  font-size: 1rem;
  color: $brand-text;
}

// ----------------------------------------------------------
// 6. Post list — clean, no card, subtle separators
// ----------------------------------------------------------
.post-block {
  background: transparent;
  border-radius: 0;
  box-shadow: none;
  border: none;
  border-bottom: 1px solid $brand-border;
  margin-bottom: 0;
  padding: 28px 0;
  transition: none;
  text-align: left;
}

.post-block:hover {
  transform: none;
  box-shadow: none;
}

.post-block:first-child {
  padding-top: 8px;
}

.post-block + .post-block {
  margin-top: 0;
}

// Post titles
.post-title {
  font-size: 1.2rem;
  font-weight: 600;
  line-height: 1.4;
  letter-spacing: -0.01em;
  text-align: left;
  margin-bottom: 6px;
}

.post-title a {
  color: $brand-text;
  transition: color 120ms ease;
  border-bottom: none;
}

.post-title a:hover {
  color: $brand-primary;
}

// Post meta
.post-meta {
  color: $brand-muted;
  font-size: 0.8rem;
}

.post-meta-item,
.post-meta-item a {
  color: $brand-muted;
  transition: color 120ms ease;
  border-bottom: none;
}

.post-meta-item a:hover {
  color: $brand-primary;
}

// Post excerpt
.post-body p {
  color: $brand-body-text;
  line-height: 1.65;
  font-size: 0.95rem;
}

// Read more
.post-button {
  margin-top: 10px;
}

.post-button a {
  color: $brand-primary;
  border: none;
  background: none;
  border-radius: 0;
  padding: 0;
  transition: color 120ms ease;
  font-size: 0.85rem;
  font-weight: 500;
  border-bottom: none;
}

.post-button a:hover {
  color: $brand-primary-hover;
  background: none;
  border-color: transparent;
}

// ----------------------------------------------------------
// 7. Pagination
// ----------------------------------------------------------
.pagination {
  margin-top: 32px;
  border-top: 1px solid $brand-border;
  padding-top: 24px;
}

.pagination .page-number {
  border-radius: $brand-radius-sm;
  color: $brand-muted;
  transition: all 120ms ease;
  min-width: 34px;
  height: 34px;
  line-height: 34px;
  font-size: 0.85rem;
  border: 1px solid transparent;
}

.pagination .page-number.current {
  background: $brand-primary;
  color: #ffffff;
  border-color: $brand-primary;
}

.pagination .page-number:hover:not(.current) {
  background: $brand-primary-light;
  color: $brand-primary;
  border-color: $brand-primary-soft;
}

// ----------------------------------------------------------
// 8. Post detail
// ----------------------------------------------------------
.post-body {
  line-height: 1.8;
  color: $brand-text;
  font-size: 0.975rem;
}

.post-body h2 {
  font-size: 1.35rem;
  font-weight: 700;
  color: $brand-text;
  border-bottom: 1px solid $brand-border;
  padding-bottom: 6px;
  margin-top: 40px;
  margin-bottom: 14px;
}

.post-body h3 {
  font-size: 1.12rem;
  font-weight: 600;
  color: $brand-text;
  margin-top: 28px;
  margin-bottom: 10px;
}

// Links
.post-body a {
  color: $brand-primary;
  transition: color 120ms ease;
  text-decoration: none;
  border-bottom: 1px solid $brand-primary-soft;
}

.post-body a:hover {
  color: $brand-primary-hover;
  border-bottom-color: $brand-primary;
}

// Inline code
.post-body code {
  background: $brand-code-bg;
  color: $brand-code-text;
  border-radius: 3px;
  padding: 1.5px 5px;
  font-size: 0.88em;
  border: none;
  font-family: "SF Mono", "Fira Code", "Fira Mono", "Roboto Mono", Menlo, Consolas, monospace;
}

// Code blocks
.post-body pre,
figure.highlight,
.highlight {
  background: $brand-code-block-bg;
  border-radius: $brand-radius;
  border: none;
  font-family: "SF Mono", "Fira Code", "Fira Mono", "Roboto Mono", Menlo, Consolas, monospace;
  font-size: 0.85rem;
  line-height: 1.65;
}

figure.highlight figcaption,
.highlight .gutter {
  color: $brand-code-caption;
}

// Blockquote
.post-body blockquote {
  border-left: 3px solid $brand-primary;
  background: $brand-primary-light;
  color: $brand-body-text;
  padding: 12px 20px;
  margin: 20px 0;
  border-radius: 0 $brand-radius-sm $brand-radius-sm 0;
}

// Tables
.post-body table {
  border-collapse: collapse;
  width: 100%;
  margin: 20px 0;
  font-size: 0.875rem;
}

.post-body th {
  background: $brand-primary-light;
  color: $brand-text;
  font-weight: 600;
  padding: 8px 12px;
  border: 1px solid $brand-border;
  text-align: left;
  font-size: 0.85rem;
}

.post-body td {
  padding: 8px 12px;
  border: 1px solid $brand-border;
  font-size: 0.85rem;
}

.post-body tr:nth-child(even) td {
  background: $brand-table-alt-bg;
}

// Images
.post-body img {
  border-radius: $brand-radius-sm;
  border: 1px solid $brand-border;
  max-width: 100%;
  box-shadow: none;
}

// ----------------------------------------------------------
// 9. TOC — clean docs-style
// ----------------------------------------------------------
.post-toc .nav .nav-item .nav-link {
  color: $brand-muted;
  transition: all 120ms ease;
  border-radius: $brand-radius-sm;
  padding: 3px 8px;
  font-size: 0.85rem;
  border-left: 2px solid transparent;
}

.post-toc .nav .nav-item .nav-link:hover {
  color: $brand-primary;
  background: transparent;
}

.post-toc .nav .active .nav-link {
  color: $brand-primary;
  background: $brand-primary-light;
  font-weight: 500;
  border-left-color: $brand-primary;
}

.post-toc .nav .nav-number {
  color: $brand-muted;
}

// ----------------------------------------------------------
// 10. Footer
// ----------------------------------------------------------
.footer {
  color: $brand-muted;
  font-size: 0.8rem;
  border-top: 1px solid $brand-border;
  padding-top: 24px;
  margin-top: 48px;
}

.footer a {
  color: $brand-muted;
  transition: color 120ms ease;
  border-bottom: none;
}

.footer a:hover {
  color: $brand-primary;
}

// ----------------------------------------------------------
// 11. Tags & Categories
// ----------------------------------------------------------
.tag-cloud a {
  color: $brand-menu-text;
  border-radius: $brand-radius-sm;
  transition: all 120ms ease;
  padding: 3px 8px;
  font-size: 0.85rem;
}

.tag-cloud a:hover {
  color: $brand-primary;
  background: $brand-primary-light;
  border-color: $brand-primary-soft;
}

.category-list a {
  color: $brand-text;
  transition: color 120ms ease;
  border-bottom: none;
  font-size: 0.95rem;
}

.category-list a:hover {
  color: $brand-primary;
}

.category-list-count {
  color: $brand-muted;
}

// ----------------------------------------------------------
// 12. Archive
// ----------------------------------------------------------
.posts-collapse .post-header a {
  color: $brand-text;
  transition: color 120ms ease;
  border-bottom: none;
}

.posts-collapse .post-header a:hover {
  color: $brand-primary;
}

// ----------------------------------------------------------
// 13. Search
// ----------------------------------------------------------
.local-search-popup .search-result-list li {
  border-radius: $brand-radius-sm;
  transition: background 120ms ease;
}

.local-search-popup .search-result-list li:hover {
  background: $brand-primary-light;
}

.local-search-popup .search-keyword {
  color: $brand-primary;
  background: $brand-primary-soft;
  border-radius: 3px;
}

// ----------------------------------------------------------
// 14. Back to top
// ----------------------------------------------------------
.back-to-top {
  background: $brand-primary;
  color: #ffffff;
  border-radius: 50%;
  box-shadow: 0 2px 6px $brand-primary-shadow;
  transition: all 150ms ease;
}

.back-to-top:hover {
  background: $brand-primary-hover;
  transform: translateY(-2px);
}

// ----------------------------------------------------------
// 15. Content width
// ----------------------------------------------------------
.content-wrap {
  width: 73%;
}

.main-inner {
  max-width: 860px;
}

// ----------------------------------------------------------
// 16. Post header — override theme centering
// ----------------------------------------------------------
.main-inner .posts-expand .post-header {
  text-align: left;
}

.main-inner .post-meta {
  justify-content: flex-start;
}

.post-header h1 {
  font-size: 1.6rem;
  font-weight: 700;
  letter-spacing: -0.02em;
  line-height: 1.3;
}

// ----------------------------------------------------------
// 17. Misc
// ----------------------------------------------------------
.post-meta-divider {
  color: $brand-border;
}

// ----------------------------------------------------------
// 18. Scrollbar
// ----------------------------------------------------------
::-webkit-scrollbar {
  width: 6px;
}
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb {
  background: $brand-scrollbar;
  border-radius: 3px;
}
::-webkit-scrollbar-thumb:hover { background: $brand-scrollbar-hover; }

// ----------------------------------------------------------
// 19. Responsive
// ----------------------------------------------------------
@media (max-width: 768px) {
  .content-wrap { width: 100%; }
  .post-block { padding: 20px 0; }
}
